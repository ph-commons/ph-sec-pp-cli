version: 2
project_name: ph-sec-pp-cli
changelog:
  disable: true
builds:
  - id: ph-sec-pp-cli
    main: ./cmd/ph-sec-pp-cli
    binary: ph-sec-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/ph-commons/ph-sec-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: ph-sec-pp-mcp
    main: ./cmd/ph-sec-pp-mcp
    binary: ph-sec-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
